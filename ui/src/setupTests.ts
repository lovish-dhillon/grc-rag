// Extends Vitest's `expect` with the DOM matchers (toBeInTheDocument, toBeDisabled, …) and
// registers automatic cleanup after each test via @testing-library/react.
import '@testing-library/jest-dom/vitest'
